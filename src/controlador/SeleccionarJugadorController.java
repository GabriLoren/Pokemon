package controlador;

import javafx.fxml.FXML;

import javafx.fxml.FXMLLoader;
import javafx.scene.Parent;
import javafx.scene.Scene;
import javafx.scene.control.Button;

import javafx.scene.control.TextField;
import javafx.stage.Stage;
import modelo.Trainer;

import java.io.IOException;

import cargar.CargarEntrenador;

import cargar.CargarPokemons;
import javafx.event.ActionEvent;

public class SeleccionarJugadorController {
	@FXML
	private TextField TxtInsertarJugador;
	@FXML
	private Button btbcargarEntrenador;

	// Event Listener on Button[#btbcargarEntrenador].onAction
	@FXML
	public void cargarEntrenador(ActionEvent event) throws IOException {
		// TODO Autogenerated

		String entrenador = TxtInsertarJugador.getText().toUpperCase();

		CargarEntrenador.cargarEntrenador(entrenador);

		System.out.println(CargarEntrenador.getEntrenador().toString());

		CargarPokemons.cargarPokemonDesdeBbDd();

//abrir la siguiente ventana
		FXMLLoader loader = new FXMLLoader(getClass().getResource("/vista/ElegirAccion.fxml"));

		Parent root = loader.load();

		Scene scene = new Scene(root);

		Stage stage = new Stage();
//		stage.initModality(Modality.APPLICATION_MODAL); (hace que la ventana sea modal)

		stage.setScene(scene);
		stage.show();
	}
}
